# ieeg-wm — core dependencies
# Python 3.11 recommended (tested with 3.11.9).
# Pinned to major.minor; patch versions float to receive security fixes.

numpy>=1.26,<2.0
scipy>=1.12,<2.0
pandas>=2.1,<3.0
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0

# MNE ecosystem
mne>=1.6,<2.0
mne-bids>=0.14,<1.0

# Neuroimaging
nibabel>=5.1,<6.0
nilearn>=0.10,<1.0
nitime>=0.10,<1.0

# Machine learning
scikit-learn>=1.4,<2.0

# Statistics
astropy>=6.0,<7.0
statsmodels>=0.14,<1.0

# Jupyter
jupyter>=1.0,<2.0
ipykernel>=6.0

# ── External — not pip-installable ────────────────────────────────────────
# decoding_toolbox (required for circ_corr_fun and decode_with_psd_permutation):
#   git clone https://github.com/alepebel/decoding_toolbox
#   Then add decoding_toolbox/Helper_funcs/ to sys.path or PYTHONPATH.

# ── Optional ──────────────────────────────────────────────────────────────
# Interactive epoch rejection visualization:
#   pip install autoreject

# Deep-learning (ForwardEncoding notebook):
#   pip install tensorflow>=2.16,<3.0
